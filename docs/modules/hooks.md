# `harness.hooks`

Typed lifecycle events and an ordered `HookRunner` with `block`-aware
short-circuit semantics. The structural seam used by policy,
contracts, telemetry, and the privacy boundary.

::: harness.hooks
