# `harness.tools`

Pydantic-backed `Tool` definitions and an async `Dispatcher` that
validates inputs, executes handlers, and surfaces tool errors as
`ToolResult(is_error=True)` rather than raising.

::: harness.tools
