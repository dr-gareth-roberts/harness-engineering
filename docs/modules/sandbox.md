# `harness.sandbox`

`PathScope` + `PathPolicy` for filesystem-scoped tool calls and
`safe_subprocess_run` with a scrubbed environment and timeout. Keeps
sandbox concerns out of the tool handlers themselves.

::: harness.sandbox
