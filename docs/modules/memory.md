# `harness.memory`

`SessionRecord` (Pydantic JSON-serializable trajectory snapshot),
`MemoryStore` protocol, `InMemoryStore` and `FileStore`
implementations, and a `Session` helper that snapshots after every
turn.

::: harness.memory
