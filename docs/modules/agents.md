# `harness.agents`

`SubAgent` (a model + system prompt + tool allow-list) and the
`Orchestrator` that drives one through a runner, emitting lifecycle
hooks (`SessionStart` / `SessionEnd` / `PromptSubmit`).

::: harness.agents
